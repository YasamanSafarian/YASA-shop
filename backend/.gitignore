node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Build output (compiled at deploy time)
/dist

# Uploaded product images (runtime data, not source)
/uploads
